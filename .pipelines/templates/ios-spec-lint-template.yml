steps:
- task: Bash@3
  displayName: 'Spec Lint'
  inputs:
    targetType: 'inline'
    script: |
      # Write your commands here
      pod trunk lint  ./source/ios/tools/AdaptiveCards.podspec --allow-warnings
